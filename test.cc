#include <iostream>

#include "lib_playlist.h"

int main() {


    
    
    
    // for (auto a : f.metadata) {
    //     std::cout << a.first << ": " << a.second << std::endl;
    // }
    
    return 0;
}